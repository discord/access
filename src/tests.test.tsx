import {describe, it, expect} from 'vitest';

describe('testingSuite', () => {
  it('should pass', () => {
    expect(true).toBeTruthy();
  });
  it('should fail', () => {
    expect(false).toBeTruthy();
  });
});
